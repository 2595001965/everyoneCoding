{
  "name": "@ec/package-kit",
  "version": "0.1.0",
  "private": true,
  "description": ".ecpkg 归档读写",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@ec/ai": "workspace:*",
    "@ec/core": "workspace:*",
    "@ec/memory": "workspace:*",
    "@ec/shell-api": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "vitest": "^2.1.4"
  }
}
