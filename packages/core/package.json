{
  "name": "@ec/core",
  "version": "0.1.0",
  "private": true,
  "description": "应用内核：事件总线 / 命令系统 / 撤销重做 / 设置 / 日志 / 文件服务",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "browser": "./src/browser.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@ec/data": "workspace:*",
    "@ec/shell-api": "workspace:*",
    "zod": "^3.23.8",
    "zustand": "^5.0.1",
    "immer": "^10.1.1",
    "zundo": "^2.3.0"
  },
  "devDependencies": {
    "vitest": "^2.1.4"
  }
}
