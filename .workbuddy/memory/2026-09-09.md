# 2026-09-09 工作记录

## Wave 1 复核与续作

- Wave 1 核心、适配器与设置组件已产生，但此前标注“全部落地”不准确；任务总表已改回“进行中”。桌面端尚未提供 `__EC_AI_SETTINGS__` 实例注入，E2E-10/11 不能以单元测试通过替代验收。
- 生产构建暴露 `@ec/ai` 统一入口将 Node 网络/SQLite 带入浏览器的问题；新增 `packages/ai/src/browser.ts`，通过同一包名的 browser 条件入口隔离平台依赖，renderer Vite 别名同步调整，构建复测成功。跨包仍走单一包入口。
- 连接测试改为模型发现异常时回退手填模型，最小对话成功才判成功；中断/partial不能误判成功。AI 错误消息增加脱敏、不保留底层 cause；新增三项回归测试通过。
- 19:51 后继续审计并补强：Electron preload 的 AI stream 增加 requestId 过滤与请求参数校验；Tauri bridge 改为 Channel 转发并对启动错误显式发出错误事件；新增 `testDraftConnection` 让设置页连接测试可在保存前使用草稿 Key；队列按 ticket 复用 active release 并保证 release 幂等。
- 新增 `apps/desktop-electron/src/main/__tests__/ai-runtime.test.ts`，验证 DPAPI 模拟密文往返和路径穿越拒绝。根目录最终测试 62 文件 / 373 项通过，五个关键 TypeScript 项目检查通过。
- 发现并记录构建环境限制：仓库当前没有可用 `node_modules/.bin/esbuild` shim，直接调用 pnpm 脚本返回 CommandNotFound；此前用 pnpm store 内 esbuild 可构建主进程但 CJS 下存在 import.meta 警告。
