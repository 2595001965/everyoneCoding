{
  "name": "@ec/account",
  "version": "0.1.0",
  "private": true,
  "description": "账号客户端与云端最小服务对接",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@ec/core": "workspace:*",
    "@ec/shell-api": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.4"
  }
}
