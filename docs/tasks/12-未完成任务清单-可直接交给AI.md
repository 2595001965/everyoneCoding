# EveryoneCoding 未完成任务清单（可直接交给 AI）

> 生成日期：2026-09-20
>
> 依据：`docs/PRD-EveryoneCoding.md` v1.3、`docs/tasks/00~11`、`.workbuddy/memory/MEMORY.md`、
> `.workbuddy/memory/2026-09-19.md`、`docs/ACCEPTANCE-REPORT.md`、`docs/PERF-REPORT.md`，以及当前工作树代码。
>
> 这份清单是当前状态的补充执行清单。`docs/tasks/01~11` 是历史任务卡，里面的 `[ ]` 不能单独证明当前任务未完成；本文件以代码、运行时返回值和验收报告中的最新证据为准。

## 一、当前结论

### 已完成，不再重复投递

- Wave 0~10 的大部分领域内核、渲染组件、测试和接口契约已经存在。
- Electron 主进程已经真实启动，SQLite 迁移、AI 主进程运行时、settings/workspace/docs/auth 四个域已装配；四域共 69 个方法在 2026-09-18 已通过真实测试。
- Tauri 外壳已经编译、打包并实机启动；Rust `cargo check`、`cargo clippy -- -D warnings`、`cargo build`、NSIS 产出和渲染冒烟已通过。
- 全仓测试、类型检查和 lint 曾达到全绿；环境并发导致的性能假红已经有单独记录，不能据此宣称下面的真实外壳验收已完成。
- `.ecpkg` 核心容器、导入导出、重命名内核、AI 协议适配器、设计器和流水线纯领域逻辑已有实现与测试。

### 仍未完成或没有充分证据证明完成

1. Tauri 业务域和 AI 在 `bridge.ts` 中仍明确返回 `NOT_SUPPORTED`，不满足 PRD D-01 的双形态功能等价。
2. Electron 渲染层仍只装配四个基础域；记忆、设计器端口、AI 上下文/代码、流水线、Git、预览、导航、重命名、用量和归档端口没有生产运行时注入，多个页面仍显示“未初始化”。
   （2026-09-20 更新：T12-01 已注入十一域端口；T12-02 已把 memory / ai-context / code / designer
   四个域从"数据直供 / 如实拒答"推进到真实主链路，并给上下文面板与代码视图补上了生产挂载页。
   仍待完成：流水线、Git、预览、导航、重命名、归档与用量的生产主链路见 T12-03 / T12-04 / T12-05。）
3. Electron 工作台的流水线阶段、最近 Git 提交和项目缩略图仍是空实现或 `null`。
4. 文档域没有注入 AI 摘要端口，图片 OCR 没有 OCR 引擎；FR-DOC-01/04 不能宣称完成。
5. 邮箱验证/找回密码没有服务端邮件投递与验签实现；OAuth 自定义协议辅通道也没有接线。另有客户端/服务端 OAuth、绑定响应字段需要契约对齐。
6. 自动更新在 Electron 主进程传入 `updater: null`，Tauri `plugins.updater.pubkey` 仍为占位符，更新端点也不是可发布配置。
7. 七端真实代码生成/编译、双形态安装包的性能数据、人工“全程不打开终端”走查、真实 OAuth/邮箱/服务端容器验证没有充分证据。
8. Python/Java 出现位置索引目前默认使用内置降级解析器；需要接入真实 `libcst`/JavaParser 或明确并验证产品可接受的降级策略。
9. CI 的覆盖率步骤仍设置 `continue-on-error: true`，没有满足“覆盖率不达标即构建失败”的门禁语义。

## 二、执行规则

把下面每一张任务卡单独发送给一个 AI 会话。AI 必须先读取本文件、PRD、相关历史任务卡和 `.workbuddy/memory/MEMORY.md`，再检查当前代码，不得假定旧报告仍然准确。

每张任务完成时必须：

1. 只修改该任务范围内的文件，保留用户已有改动，不执行破坏性 git 操作。
2. 为真实行为补测试；不能用 mock 或“返回成功”掩盖未接入能力。
3. 运行任务卡列出的验证命令；命令因环境缺失无法运行时，记录准确原因、已运行替代验证和剩余人工步骤。
4. 更新相关验收文档，并在最终回复中列出改动文件、测试结果和仍需外部条件的事项。
5. 跨进程参数只能传可结构化克隆的数据；中途进度使用 `ec:domain:event`，不可把回调函数塞进 IPC 参数。

推荐顺序：`T12-01 → T12-02 → T12-03 → T12-04 → T12-05 → T12-06 → T12-07 → T12-08`，然后并行执行 `T12-09~T12-12`，最后执行 `T12-13~T12-15`。

## 三、P0：必须补齐的产品功能

## T12-01 Electron 生产端口总装与项目上下文

### 进度（2026-09-20）：**已完成**（主进程装配 + 渲染层适配器 + 项目上下文 + 集成测试）

**主进程侧已完成**（`pnpm lint` / `pnpm -r typecheck` / `pnpm test` / `pnpm test:e2e` /
`pnpm quality-gate` 六模块全绿）：

- `packages/shell-api/src/domain-control.ts`：`DOMAIN_KINDS` 由 4 扩到 15；补齐
  `isDomainKind` 守卫（此前被 `index.ts` 导出但未定义）；`DOMAIN_RPC_METHODS` 新增十一域方法白名单；
  新增 6 个域事件常量 + `isKnownDomainEventPayload(type, payload)` 按 `type` 分发的载荷守卫注册表。
- `apps/desktop-electron/src/main/domain/domain-factories.ts`（新）：`createProductionDomains(ctx)`
  总装十一域路由，返回 `{ routers, disposers }`。
- `apps/desktop-electron/src/main/domain/domains/`（新，11 个文件）：memory / pipeline / git / preview /
  rename / ai-context / code / nav / designer / usage / package 的真实域实现，
  复用 `@ec/memory`、`@ec/pipeline`、`@ec/git`（含 Merge/Conflict/Recovery/Remote/History 五个服务）、
  `@ec/preview`、`@ec/package-kit`，不重写领域内核。
- `apps/desktop-electron/src/main/index.ts`：`buildDomainRuntime` 增加 `aiStack` 参数并挂载生产路由；
  本轮 AI 传 `null`（T12-08 再接），域内生成类方法如实报 `NOT_SUPPORTED` 而不伪造结果。
- `apps/desktop-electron/package.json`：补 `@ec/git`/`@ec/memory`/`@ec/pipeline`/`@ec/preview`/`@ec/registry`
  五个 workspace 依赖。

### 进度（2026-09-20 · 第二轮）：渲染层接线收尾 **已完成**

下面五项**已于本轮全部关闭**，逐项落地情况：

1. **渲染层适配器**（已完成）：`apps/renderer/src/runtime/production-ports.ts` 为
   memory/pipeline/git/preview/rename/package/usage/ai-context/code/nav/designer 十一域建好适配器
   并写入 `__EC_*__` 槽位。该文件另有一处限流留下的**交错损坏**（`createPipelineApi` 与
   `createGitApi` 互相穿插、无法编译），已按行区间重排修正。
2. **同步签名口**（已定方案并落地）：采用 **(a) 新增同步通道**，五处同步 ——
   `shell-api` 的 `DOMAIN_SYNC_METHODS` + `isDomainSyncMethod` + `DomainControlHost.invokeSync?`
   → `createDomainRuntime.invokeSync`（复用同一套白名单与脱敏）+ `SyncDomainRouter` 带 `ctx`
   → IPC 通道 `ec:domain:invokeSync`（`ipcMain.on` + `event.returnValue`）
   → preload `domain.invokeSync` → 渲染层 `createDomainSyncCaller`。
   外壳没有该能力时**不注入**这两个端口（Tauri / mock 如实降级），而不是读快照脏数据。
   顺带修掉一个只在真机现形的缺陷：`registerAllIpc` 的包装对象此前**没转发 `ipc.on`**，
   同步通道在生产里从未注册（渲染层 `sendSync` 会永久阻塞）。
3. **项目上下文贯穿**（已完成）：新增 `apps/renderer/src/runtime/project-context.ts`；
   `WorkspacePage.openProject` 先取真实项目摘要再写入上下文；`DesignerPage` 以 `key={project.id}`
   装载会话（切项目即卸载旧订阅与旧状态）；`P1` / `U-TEST` / `createLoginPageDsl()` 已从实现代码移除。
4. **端口级集成测试**（已完成，33 项）：
   `apps/desktop-electron/src/main/__tests__/domain-production.test.ts`（15 项）与
   `apps/renderer/src/runtime/__tests__/production-ports.test.ts`（18 项），覆盖
   真实项目 ID 贯穿、重启后可重读、错误码映射、并发项目不串数据。
5. **`docs/ACCEPTANCE-REPORT.md`**（已完成）：见 §2.7，含每端口装配证据表、同步通道五层落地表、
   12 处真实缺陷清单与门禁实测数据。

**本轮修掉的真实缺陷**（12 处，明细见 `docs/ACCEPTANCE-REPORT.md §2.7.3`）。其中三处只在真实外壳
或真实数据下现形：① 同步通道从未注册（渲染层会卡死）；② code 域外部改动事件发进 no-op `emit` 黑洞；
③ `designer.createPage` 产出的 DSL 缺 5 个必填字段 —— 会让**新建项目打开设计器直接失败**。

**仍未完成（外部条件）**：`pnpm dev:electron` 的人工 GUI 走查需真实桌面会话，沙盒内无法操作；
同语义场景已由集成测试以真实 SQLite + 真实工程目录覆盖（换运行时重读 / 双项目交替写入）。

> 以下为**历史遗留清单原文**（保留以便追溯上一轮的判断依据）：

1. **渲染层适配器**：`apps/renderer/src/runtime/domain-ports.ts` 目前只注入 workspace/docs/auth/settings。
2. **同步签名口的设计决定（必须先定，再写代码）**：`MemoryApi` 与 `PipelineApi` 是**同步签名**，
   而 `DomainControlHost.invoke` 只有异步口。
3. **项目上下文贯穿**：`WorkspacePage` 的 `onOpenProject` 不传 projectId；`DesignerPage` 固定夹具。
4. **端口级集成测试**：真实项目 ID 贯穿、重启后可重读、错误码映射、并发项目不串数据。
5. **`docs/ACCEPTANCE-REPORT.md`** 补每个端口的生产装配证据。

**交付目标**：在 Electron 真窗口中打开真实项目后，所有已有领域 UI 使用真实 SQLite/文件/AI/Git 端口，不再依赖固定 `P1`、`U-TEST` 或登录页夹具；Tauri/mock 仍可按能力协商优雅降级。

**直接交给 AI 的提示词**：

```text
请在 D:\code\program\everyoneCoding 完成“Electron 生产端口总装与项目上下文”任务。

先读：docs/PRD-EveryoneCoding.md（D-01、FR-WSP、FR-DSG、FR-MEM、FR-AI、FR-PIPE、FR-GIT、FR-PRV、FR-UNI）、docs/tasks/10-Wave9-工作台设置文档账号.md、docs/tasks/11-Wave10-质量打包与验收.md、.workbuddy/memory/MEMORY.md；再阅读 apps/renderer/src/main.tsx、apps/renderer/src/runtime/domain-ports.ts、apps/renderer/src/pages/*、apps/renderer/src/features/*/(*-api*)、apps/desktop-electron/src/main/index.ts 和现有 Electron 域实现。

实现要求：
1. 保持 shell-api/领域端口契约，不在 renderer 直接 import electron、better-sqlite3 或 Node 文件系统。
2. 为 memory、designer、AI context/code、pipeline、git、preview、navigation、rename、package、usage 建立生产端口适配器；能复用现有 packages/* 领域实现就复用，不重写领域内核。
3. 把当前项目上下文贯穿 Workspace → Designer/Pipeline/Git/Preview/Rename/Docs/Memory：打开项目时传递 projectId，移除固定 P1、U-TEST 和固定登录 DSL；项目切换时清理旧订阅和旧状态。
4. 需要持久化的 DSL、页面记忆、流水线产物、Git/代码文件、重命名注册表和归档数据必须接到真实 SQLite/工程目录/现有端口；不允许把内存假实现当生产实现。
5. 在 describe/negotiate 中准确报告每个能力；缺少 Tauri 能力时保留明确降级，不伪造成功。
6. 为每个生产端口加最小集成测试，至少覆盖：真实项目 ID 贯穿、重启后能重新读取、端口错误码映射、并发项目不会串数据。

验收：
- 启动 pnpm dev:electron，创建两个项目，分别打开设计器并修改，重启后内容不串且仍存在。
- 工作台、设计器、记忆、流水线、Git、预览、重命名、归档和用量页面不再因为“端口未注入”显示占位（确实需要外部条件的功能显示结构化引导）。
- 运行 pnpm lint、pnpm -r typecheck、相关包测试、pnpm test:e2e；修复所有由本任务引入的失败。
- 更新 docs/ACCEPTANCE-REPORT.md，列出每个端口的生产装配证据。
```

## T12-02 记忆、上下文、代码写入与设计器端口

### 进度（2026-09-20）：**已完成**（四个域的真实主链路 + 渲染层挂载点 + 集成测试）

**交付情况逐条对照任务卡**：

1. **主进程真实装配**（已完成）：`memory` 域此前已是真实 SQLite 读写；
   本轮把另外三处的"数据直供 / 明确拒答"换成真实实现 ——
   `ai-context` 域重写为**真实 `ContextEngine` 组装**（十类块，五个数据端口全部落在
   业务 SQLite 与工程目录上）；`code` 域接 `WritePipeline` 的 `plan → preview → apply`
   三段式事务、Code Anchor 写回与外部改动检测；`designer` 域的页面记忆 / 路由总表
   改走 `@ec/memory` 的 `PageMemoryService` / `ProjectMemoryService.mergeRoutes`。
2. **上下文真实性与 skipped/source**（已完成）：五层记忆按层级收敛（长期记忆按用户级、
   页面/元素/功能按本次目标收敛）、元素祖先链来自页面 DSL、备注来自 `note` 表、
   文档按 `sections_json` 章节切片、代码来自 `code_anchor` + 真实文件；
   两条机器化断言防"空块伪装"：① 每块 `content` 非空 ⟺ `items` 非空；
   ② 空项目组装时只有 `instruction` 有内容且其余块给出 `skipped` 原因。
3. **代码只读与写入管线**（已完成）：三种写入模式全部经 `WritePipeline`
   （计划期算 before/after、应用前重新比对磁盘做冲突检测、任一失败整体回滚）；
   `requestRework` 走真实模型 → 输出契约解析 → 计划经新的 `code:write-plan`
   域事件回流 UI 预览；域内**不存在**任何"保存代码"方法。
4. **设计器端口**（已完成）：`writePageStructure` 现在是"精简 → 页面记忆 → 增量 revision"，
   重复保存不刷台账；路由总表走 `ProjectRouteMemoryPort`；AI 生成页面返回领域校验结论。
5. **浏览器入口纯净**（已完成）：新增 `@ec/designer/notes` 纯子入口（主进程复用备注
   领域规则，不带 React），`vite build` 通过且 `CodePage` 为独立 chunk。

**本轮修掉的真实缺陷（4 处，详见 `docs/ACCEPTANCE-REPORT.md §2.8.3`）**：
① `note` 表结构与领域模型不匹配（迁移 `0006`）；② `element` / `feature` 行从未登记
（`code_anchor.element_id`、`memory_item.feature_id` 的外键因此必然失败）；
③ Windows 上 `fs.watch` 的 `filename` 报的是目录名 ⇒ 外部改动检测改为"事件触发 + 索引比对"；
④ `CodeView` 装载后不选中任何文件。

**新增测试**：`apps/desktop-electron/src/main/__tests__/domain-content-ports.test.ts`（17 项）、
`apps/renderer/src/pages/__tests__/code-page.test.tsx`（5 项）、
`packages/designer/src/__tests__/notes-entry-purity.test.ts`（3 项）。

**门禁**：`pnpm lint` / `pnpm -r typecheck` 零问题；相关包测试全绿；
`vite build` 4.23s 通过。根级全量 **239 文件 / 2444 项，2443 通过、1 红**：
红项是 `@ec/ai` 的上下文性能基准（全量并发下 p95 338.56ms > 300ms），
**已用"只改并发度"的对照判定为环境性假红**（单独跑该文件 p95 = 6.95 / 7.99ms，余量约 40 倍；
对照表与处置口径见 `docs/TEST-REPORT.md §5.1`）。**未改动任何性能/超时预算**。

**仍未完成（外部条件）**：`pnpm dev:electron` 的人工 GUI 走查需真实桌面会话；
「代码与上下文」页的交互已由测试覆盖，但"在真窗口里点一遍"仍需主人执行一次。

**现状证据**：`packages/ai/src/context/blocks/*` 明确把记忆、文档、代码、设计器端口缺失记为“未接入”；`packages/designer/src/store/ports.ts` 只有注入契约；`MemoryPage` 在没有 `__EC_MEMORY__` 时显示初始化引导。

**交付目标**：完成 Electron 侧 `@ec/memory`、`@ec/ai`、`@ec/designer` 端口装配，让 FR-AI-01~~11、FR-MEM-01~~22、FR-DSG-11/12 的真实主链路可运行。

**直接交给 AI 的提示词**：

```text
请在 D:\code\program\everyoneCoding 完成“记忆、上下文、代码写入与设计器端口”任务。

先读：docs/PRD-EveryoneCoding.md 的 FR-MEM、FR-AI、FR-DSG-11/12、FR-ANN；docs/tasks/03-Wave2-记忆系统.md、docs/tasks/05-Wave4-上下文引擎与代码生成.md、docs/tasks/04-Wave3-可视化设计器.md；再读 packages/memory、packages/ai/context、packages/ai/write、packages/ai/anchors、packages/designer/src/store/ports.ts、apps/renderer/src/features/memory、features/ai、features/code 和 apps/renderer/src/features/designer。

实现要求：
1. 在 Electron 主进程以现有业务 SQLite、工程文件和 AI gateway 装配 MemoryApi、ContextPanelApi、CodeViewApi、DesignerPorts；渲染层只消费既有端口。
2. 上下文必须真实读取长期/项目/功能/页面/问题记忆、备注、文档片段、元素祖先链、代码和 Code Anchor，并保留 skipped/source 信息；不能把空块伪装成已有内容。
3. 代码视图保持只读；所有新建文件、diff 补丁和 AI 重改均走现有 WritePipeline 的 plan → preview → apply 事务，外部改动检测和失败回滚必须保留。
4. 设计器修改页面后，调用 PageMemoryPort 增量写页面记忆；路由变化调用 ProjectRouteMemoryPort；AI 生成页面走真实模型端口并校验 DSL。
5. SQLite/Node 依赖不能进入 renderer 浏览器入口；必要时补 browser.ts 和单一包入口。

验收：
- 新建页面并修改元素，页面记忆出现对应结构摘要；打开上下文面板能看到真实来源和裁剪提示。
- 选中元素生成代码，能预览 diff、应用、回滚；手动键入/粘贴/拖拽编辑代码均被拦截。
- 外部进程修改代码后，客户端提示重新生成或回滚。
- 补充真实端口集成测试，并运行相关包测试、pnpm lint、pnpm -r typecheck、pnpm build:renderer。
```

## T12-03 流水线持久化与 S1→S7 运行时

**现状证据**：`apps/desktop-electron/src/main/domain/workspace.ts` 的注释说明没有 `@ec/pipeline` 持久化装配时 `getProjectStage()` 返回 `null`；`apps/renderer/src/pages/PipelinePage.tsx` 没有 `__EC_PIPELINE__` 就显示初始化引导。

**直接交给 AI 的提示词**：

```text
请在 D:\code\program\everyoneCoding 完成“Electron 流水线生产运行时”任务。

先读 PRD §7、FR-PIPE-01~13，docs/tasks/06-Wave5-开发流水线.md，packages/pipeline 全部入口和测试，apps/renderer/src/features/pipeline/*，apps/desktop-electron/src/main/domain/workspace.ts 与 SQLite schema/migrations。

实现：
1. 用现有 PipelineMachine、ArtifactStore、S1/S3/S4/S5 阶段实现和 recovery 逻辑，新增 Electron 主进程持久化适配器；状态、阶段产物版本、activeVersion、stale、断点进度和用户技术选型必须重启后恢复。
2. 把 S1→S7 端口注入 renderer，PipelinePage 使用真实 projectId/userId/projectName，不再固定 P1/U-TEST/商城。
3. S3 前没有 TechChoice 时必须阻断；选择结果写项目记忆；S5 队列支持单节点重试/跳过/暂停和域事件进度。
4. 产物写入文档库/项目目录的现有官方位置，失败事务不留半成品；不把 UI 本地 textarea 当作持久化实现。

验收：
- 输入需求走完 S1→S5，关闭并重启 Electron 后从上次阶段继续；每阶段可回看版本、diff、回退和下游 stale。
- 未完成技术选型不能进入 S3；S5 单节点失败不阻塞其余节点。
- 增加主进程集成测试和一条真实 E2E；运行 pnpm lint、pnpm -r typecheck、相关测试和 pnpm test:e2e。
```

## T12-04 Git、预览、导航和统一重命名生产端口

**现状证据**：renderer 的 Git/Preview/Rename 页面在没有全局端口时直接显示“服务未初始化”；`workspace.ts` 的 dashboard Git 明细固定返回空列表；预览页有联动导航占位；重命名页只有 UI 和端口契约。

**直接交给 AI 的提示词**：

```text
请在 D:\code\program\everyoneCoding 完成“Git、预览、导航和统一重命名生产端口”任务。

先读 PRD 的 FR-GIT、FR-PRV、FR-NAV、FR-UNI，docs/tasks/07-Wave6-工程运行Git预览导航.md、08-Wave7-全局统一重命名.md，packages/git、packages/preview、packages/registry、packages/ai/nav，renderer 对应 feature API/UI，以及现有 Electron fs/process/domain event 能力。

实现：
1. Electron 主进程装配 GitApi（CLI/git2 回退、diff、分支、远程、冲突、stash、回滚）、PreviewApi（静态/联动/mock/真实后端/日志/API 调试）、NavApi 和 RenameApi；所有路径必须经过工程根目录安全校验。
2. 重命名执行器使用现有 registry/occurrence/transaction/anchor 代码，代码、文档、记忆、DSL、注册表和锚点必须一个事务处理，失败整体回滚；不使用未经作用域分析的全局文本替换。
3. 预览后端托管必须走受控进程端口，实时输出结构化日志、端口冲突顺延、停止进程；局域网预览显示风险并默认关闭。
4. Git 所有破坏性操作继续二次确认并建立安全快照；凭据只能走 DPAPI/SSH agent，不落日志。
5. 页面与元素的 Ctrl+点击、代码锚点反向跳转和关系图使用同一项目上下文。

验收：
- 在临时真实仓库完成 init→修改→diff→提交→分支→冲突解决→回滚→stash 全流程，无终端操作。
- 启动一个 Node/Python demo 后静态/联动预览可访问，表单请求到真实后端，API 面板可重放并复制 cURL。
- 通过 E2E-06、E2E-07、E2E-08、E2E-15、E2E-16、E2E-17；运行 lint、typecheck、相关集成测试。
```

## T12-05 归档、用量、备份和遥测生产接线

**现状证据**：`apps/renderer/src/features/package/*`、usage UI 已有契约和组件，但没有 renderer 生产端口；`package-ports.ts` 明确附件子系统尚未装配；`settings.ts` 的归档入口部分已实现，不能因此宣称完整 FR-PKG。

**直接交给 AI 的提示词**：

```text
请在 D:\code\program\everyoneCoding 完成“归档、用量、备份和遥测生产接线”任务。

先读 PRD FR-PKG-01~13、FR-SET-02/04/06，docs/tasks/09-Wave8-归档与迁移.md、10-Wave9-工作台设置文档账号.md、11-Wave10-质量打包与验收.md，packages/package-kit、packages/core/telemetry、apps/renderer/src/features/package、features/usage、features/settings，以及 apps/desktop-electron/src/main/domain/package-ports.ts/settings.ts。

实现：
1. 把 PackageApi、UsageApi、BackupSettings 通过 Electron 主进程真实注入；导出/导入、冲突预览、五种导入模式、自愈报告、增量包、定时备份、保留份数和快照回滚必须可用。
2. 补齐附件内容寻址导出/导入，或在 UI 和 manifest 中明确阻断并给出可执行引导；不能把附件静默丢掉。
3. 用量统计从真实 AI gateway usage_record 读取，预算阈值在请求前拒绝/排队，UsageDashboard 的全局/项目两级数据与源数据一致。
4. 遥测默认关闭；启用时只允许事件白名单字段，绝不写提示词、代码、文档、Key 或 Token；隐私面板清除内存、文件缓冲和数据库记录。

验收：
- 干净数据目录导出再导入后，项目、DSL、文档、记忆、锚点、注册表和附件可打开；篡改、错误口令、高版本包不会留下半导入状态。
- 定时备份按日/周生成并按保留数清理，可一键恢复；全量/项目/选中范围均有测试。
- 用量预算超限在真正调用模型前阻断；遥测 payload 断言不含内容字段。
- 运行相关测试、lint、typecheck，并更新 ACCEPTANCE-REPORT。
```

## T12-06 Tauri 双形态功能等价：域端口与 AI 栈

**现状证据**：`apps/desktop-tauri/src/bridge.ts` 的 domain/ai 能力和 `apps/desktop-tauri/src-tauri/src/commands/ai.rs` 明确返回 `NOT_SUPPORTED`；当前 Tauri 只完成 shell 基础能力。

**直接交给 AI 的提示词**：

```text
请在 D:\code\program\everyoneCoding 完成“Tauri 双形态功能等价”任务。

先读 PRD D-01、FR-ACC/FR-MEM/FR-AI/FR-PIPE/FR-GIT/FR-PRV/FR-SET，docs/tasks/01-Wave0-工程底座与内核.md、docs/RELEASE.md、apps/desktop-tauri/src/bridge.ts、apps/desktop-tauri/src-tauri/src/commands/*、state.rs、Cargo.toml，以及 Electron 对应实现。

实现要求：
1. 选择并记录一种可维护方案：把业务运行时迁入 Rust，或用受控 sidecar 承载现有 Node 领域代码；不能在 Tauri bridge 中伪造 Electron 的成功返回。
2. 首先覆盖四域（workspace/docs/auth/settings），再覆盖 memory/AI/pipeline/git/preview/rename/package/usage；共用 shell-api 契约，错误码、域事件和结构化克隆规则与 Electron 一致。
3. AI 侧至少支持 Provider/Model、DPAPI/安全存储等 Windows 能力、流式生成、usage/budget、远程配置和 WritePipeline；无能力必须 negotiate 为 false 并给出理由。
4. 添加 Rust/sidecar 生命周期、崩溃清理、路径安全和升级兼容测试；不要引入 renderer 对 Tauri API 的直接依赖。

验收：
- Tauri 实机启动后，四域每个方法至少有一条真实端到端测试；domain/ai 不再因为“尚未接入”返回 NOT_SUPPORTED。
- 运行 cargo check、cargo clippy --all-targets -- -D warnings、cargo test、pnpm build:renderer 和 Tauri 冒烟。
- 更新能力矩阵和 ACCEPTANCE-REPORT，明确仍受外部工具链限制的功能。
```

## T12-07 文档 AI 摘要、图片 OCR 与账号完整闭环

**现状证据**：`apps/desktop-electron/src/main/domain/docs.ts` 注释和代码都没有注入 `MemoryExtractionPort`；`packages/core/src/docs/parsers/image-ocr.ts` 没有 OCR 引擎；`apps/desktop-electron/src/main/domain/auth.ts` 的 `registerProtocol` 固定返回 false，且服务端没有邮箱验证/重置路由。

**直接交给 AI 的提示词**：

```text
请在 D:\code\program\everyoneCoding 完成“文档 AI 摘要、图片 OCR 与账号完整闭环”任务。

先读 PRD FR-DOC-01~06、FR-ACC-01~08 和 §8，docs/tasks/10-Wave9-工作台设置文档账号.md，packages/core/src/docs、packages/account、services/account、apps/desktop-electron/src/main/domain/docs.ts/auth.ts、renderer auth/docs 页面和 OpenAPI 文档。

实现要求：
1. 给 docs 域注入真实 MemoryExtractionPort，预览/提交“转为记忆”时调用已配置 AI，保留来源文档和段落锚点；失败必须结构化提示，不得伪造摘要。
2. 接入 Windows 可分发的 OCR 方案（本地引擎或受控 sidecar），支持图片导入、语言/失败提示和可检索文本；若依赖外部安装，提供检测和安装引导。
3. 对齐 AuthClient、Electron auth 域、服务端 OpenAPI 的请求/响应字段，尤其 identity/tokens、OAuth callback、bindings 的参数和返回结构；补真实契约测试。
4. 实现邮箱验证链接/验签、验证码重置密码、过期/重复使用/限流、邮件发送配置和开发环境可测试的邮件 sink；UI 完成注册→验证→登录→找回密码。
5. 接线 Electron 自定义协议 everyonecoding://oauth；回环失败时才回退协议，保证 state/PKCE 校验和一次性消费。

验收：
- Markdown/Word/PDF/图片各有成功和失败测试；图片导入后能搜索 OCR 文本并转记忆。
- 邮箱注册收到可验证链接，验证后状态正确；密码重置令牌单次有效且过期拒绝。
- OAuth 回环和自定义协议各有模拟回调测试；真实凭据缺失时记录手工验收步骤。
- 运行服务端测试、account/domain 测试、renderer 测试、lint 和 typecheck。
```

## T12-08 AI 远程配置启动刷新与模型生成主链路

**现状证据**：`packages/ai/src/service/ai-control-api.ts` 已有 `refreshRemoteSourcesOnBoot()`；Electron `runtime.ts` 暴露了方法，但 `apps/desktop-electron/src/main/index.ts` 没有在启动时调用；renderer 只初始化 AI 设置 API，未保证真正模型生成、上下文和设计器端口共用同一 gateway。

**直接交给 AI 的提示词**：

```text
请在 D:\code\program\everyoneCoding 完成“AI 远程配置启动刷新与模型生成主链路”任务。

先读 PRD FR-MDL-01~13、FR-AI-01~13，docs/tasks/02-Wave1-AI接入层.md、05-Wave4-上下文引擎与代码生成.md，packages/ai/src/service/ai-control-api.ts、gateway、remote-config、generate、write，apps/desktop-electron/src/main/ai/runtime.ts/index.ts 和 renderer AI/settings 端口。

实现：
1. Electron 启动时异步调用 refreshRemoteSourcesOnBoot，不阻塞窗口；失败使用缓存并在设置页显示上次结果/原因。
2. 将真实 AI Gateway 绑定到上下文组装、代码生成、设计器生成、记忆抽取、提交信息生成和流水线生成；用途化模型绑定、流式输出、中断/重试、failover、budget/limits 必须走同一配置。
3. API Key 只通过 DPAPI/secure store；日志、遥测、remote config 和归档均脱敏。远程配置可选 Ed25519 校验，用户本地配置优先。
4. 启动刷新、连接测试、模型列表、预算拒绝和失败切换都补集成测试，不使用真实 Key 作为测试数据。

验收：
- 配置可用 OpenAI/Anthropic 兼容中转完成一次流式对话、一次结构化页面生成和一次代码生成；没有配置时 UI 给出可执行引导。
- 远程源不可达不阻塞启动，缓存仍可用；签名错误不会应用配置。
- 预算超限在网络请求前拒绝，连续失败按配置切换并记录脱敏事件。
```

## 四、P1：已实现代码之外的真实环境与规范缺口

## T12-09 自动更新、签名和发布产物

**直接交给 AI 的提示词**：

```text
请完成 D:\code\program\everyoneCoding 的“自动更新、签名和发布产物”任务。

先读 PRD FR-SET-05、NFR-P-09、docs/RELEASE.md、ci/make-release.mts、electron-builder.yml、apps/desktop-electron/src/main/index.ts、apps/desktop-electron/src/main/ipc/updater.ts、apps/desktop-tauri/src-tauri/tauri.conf.json 和现有 update 包。

把 Electron electron-updater 和 Tauri updater 接入真实主进程，使用环境变量/CI secret 注入签名密钥，替换 Tauri pubkey 占位符和示例 endpoint；实现检查、增量下载、安装、重启、失败回滚和离线提示。不要把私钥写入仓库。

验收：本地可产出两种 NSIS 安装包并生成 latest.yml/latest.json/release-manifest；使用本地静态更新源完成旧版→新版检查/下载/重启；签名错误、网络失败、半包和回滚均有测试。更新 docs/RELEASE.md，记录不可在本机完成的签名发布步骤。
```

## T12-10 七端真实生成与工具链检测验收

**直接交给 AI 的提示词**：

```text
请完成 D:\code\program\everyoneCoding 的“七端真实生成与工具链检测”任务。

先读 PRD D-03、FR-AI-12/13、FR-DSG-01/14、FR-PRV-08、FR-PIPE-13、NFR-C-05、E2E-21，docs/tasks/06-Wave5-开发流水线.md 和现有 multi-platform-generator/toolchain detector。

让 Web React、Flutter Android/iOS、ArkTS/ArkUI HarmonyOS、Tauri Windows/Linux/macOS 生成真实工程；每端生成前检测工具链、版本和目标平台，缺失时返回 skipped_toolchain_missing + 安装引导/待验清单，禁止静默跳过。生成后执行最小编译或等价静态校验，失败允许 AI 重试最多 2 次并保留日志。

验收：勾选 Web+Android+HarmonyOS+Windows 后通过 E2E-21；有工具链时保存编译产物/版本，有工具链缺失时 UI 和报告准确说明原因。补矩阵测试、样例工程和文档。
```

## T12-11 外部 AST 解析器与重命名完整性

**直接交给 AI 的提示词**：

```text
请完成 D:\code\program\everyoneCoding 的“外部 AST 解析器与重命名完整性”任务。

先读 PRD FR-UNI-04~12、NFR-R-04，docs/tasks/08-Wave7-全局统一重命名.md，packages/registry/src/occurrence/ast/*、types.ts、rename-transaction 和现有 parser degradation 测试。

为 TypeScript/JavaScript、Python、Java 提供生产可用的作用域感知 AST 适配：TS/JS 使用项目已有 TypeScript API，Python 接入 libcst（或受控 Python sidecar），Java 接入 JavaParser（或受控 JVM sidecar）。保留内置降级解析器，但必须在 UI/报告显示 degradation；字符串、注释、局部同名变量、第三方同名符号不得误改。将解析器版本/能力写入诊断报告。

验收：三种语言各有正例和至少 3 个反例；1 万行影响分析 ≤1.5s（按项目口径）；失败注入时重命名整体回滚；E2E-15/16/17/20 通过。
```

## T12-12 工作台指标、缩略图和真实 Git 明细

**直接交给 AI 的提示词**：

```text
请完成 D:\code\program\everyoneCoding 的“工作台指标、缩略图和真实 Git 明细”任务。

先读 PRD FR-WSP-01/06、docs/tasks/10-Wave9-工作台设置文档账号.md、apps/desktop-electron/src/main/domain/workspace.ts、sqlite-project-store.ts、packages/git、renderer WorkspaceHome/ProjectCard/DrilldownPanel。

为 getThumbnailUrl 实现工程设计 DSL 的安全缩略图生成/缓存（不能执行不可信代码）；为 dashboard recent Git commits 接入项目代码根的 GitApi；验证记忆条目数、页面数、完成度、AI 用量/成本和 Git 提交均来自真实数据并支持下钻。100 项目使用分页/虚拟化，缩略图懒加载。

验收：创建/导入项目后工作台卡片有真实更新时间、目标端、阶段进度、缩略图占位或缩略图；仪表盘五项指标与数据库/Git 源数据断言一致；无仓库时显示空态而不是假提交；补性能测量和测试。
```

## 五、P2：质量门禁与最终验收

## T12-13 CI 门禁真正阻断失败

**直接交给 AI 的提示词**：

```text
请完成 D:\code\program\everyoneCoding 的“CI 门禁真正阻断失败”任务。

先读 docs/TEST-REPORT.md、docs/ACCEPTANCE-REPORT.md、ci/quality-gate.mts、ci/quality-gate.yml、.github/workflows/ci.yml、package.json 和所有 workspace package scripts。

修正 CI：coverage/quality-gate 不得 continue-on-error；coverage、E2E、lint、strict typecheck、Tauri clippy、版本一致性和打包 manifest 任一失败都必须退出非零。保证 Windows runner 上 pnpm 版本只由 packageManager 决定、pnpm workspace 脚本不递归误调用错误 shim。为覆盖率未取到数据、测试超时和工具链缺失分别输出可诊断失败原因。

验收：在临时故意降低覆盖率/制造 lint 错误/删除 Tauri 工具链时 CI 各自失败；恢复后全绿。更新 TEST-REPORT 和 CI 使用说明。
```

## T12-14 真实安装包性能与兼容性验收

**直接交给 AI 的提示词**：

```text
请完成 D:\code\program\everyoneCoding 的“真实安装包性能与兼容性验收”任务。

先读 PRD NFR-P-01/NFR-P-05/NFR-P-09、NFR-C-01~05，docs/PERF-REPORT.md、docs/ACCEPTANCE-REPORT.md、docs/RELEASE.md 和 perf/run.ts。

在 Windows 10 1809+ 与 Windows 11 x64 上分别安装 Electron/Tauri NSIS 包，测冷启动到可交互、空闲/大型项目内存、安装包体积、WebView2/运行库缺失引导、高 DPI 150%/200% 多屏、崩溃恢复和更新回滚。使用统一脚本/表格记录机器配置、版本、样本和原始数据，不把 jsdom 或渲染层 build 数据冒充真实外壳数据。

验收：NFR-P-01/P-05/P-09 有双形态实测结论；不达标项有可执行整改任务；产物 hash、安装日志、截图和报告可复核。
```

## T12-15 最终人工 E2E 与发布前检查

**直接交给 AI 的提示词**：

```text
请完成 D:\code\program\everyoneCoding 的最终人工验收编排，不要重新实现已经通过的领域代码。

先读 PRD §10.1、docs/E2E-CHECKLIST.md、docs/ACCEPTANCE-REPORT.md、docs/RELEASE.md 和本文件所有 T12 任务的结果。编排并执行 E2E-01~21，优先覆盖注册/验证、OAuth、需求到项目、拖拽设计、元素生成、Ctrl 跳转、Git、联动预览、问题记忆、自定义中转、远程配置、记忆生效、归档导入/冲突、重命名级联/反例/回滚、代码只读、技术选型和七端生成。

人工步骤必须全程不打开终端；记录每一步、输入、预期、实际、截图/日志路径和是否依赖真实凭据。任何失败要定位到代码缺口、环境缺失或外部服务条件，不能只写“未验证”。完成后更新 E2E-CHECKLIST、ACCEPTANCE-REPORT、PERF-REPORT、RELEASE，并输出发布阻断项清单。
```

## 六、完成判据

这份清单全部关闭的判据不是“所有旧任务卡 checkbox 被勾上”，而是同时满足：

- Electron 与 Tauri 的能力协商和实际功能与 PRD D-01 一致，缺失能力有明确可执行引导。
- renderer 不再因缺少生产端口而让核心页面停留在占位；真实项目上下文贯穿所有模块。
- FR-DOC、FR-AI、FR-PIPE、FR-GIT、FR-PRV、FR-NAV、FR-UNI、FR-PKG、FR-SET 的生产主链路有真实端到端证据。
- E2E-01~21、质量门禁、双形态安装包、性能/兼容性和人工全流程验收均有可复核结果；外部凭据或工具链导致的未执行项被单独列为发布阻断项。
- 任何报告中的“已完成”都能指向当前代码、测试输出、运行日志或安装包，而不是只指向历史计划。
