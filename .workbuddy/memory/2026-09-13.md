# 2026-09-13 工作日志

## Wave 9（工作台 / 设置 / 文档 / 账号）交付 —— T9-01 ~ T9-06

**结论**：全仓 **204 个测试文件 / 2049 项全绿**（Wave 8 基线 1858/1858-R4，本轮那 4 项真实环境用例也过了），
`pnpm -r typecheck` 17 个工程零 error，`eslint --max-warnings 0` 零问题，`apps/renderer` vite build 通过。

### 交付物（关键路径）

- 数据层：`packages/data/migrations/0005_workspace_docs.sql`（project +8 列 / document +6 列 / 新表 `doc_version`），
  `schema.ts`（含 `documentKind` 加 `imported`、新增 `docFormat` / `projectSourceKind` / `docVersionCreator` 枚举）同步。
- `@ec/core`：新增 `project/`（project-types / project-service / project-templates / git-import / doc-import，65 项测试）
  与 `docs/`（doc-types / doc-service / versioning / parsers/{markdown,txt,docx,pdf,image-ocr} + 双入口，22 项测试）；
  `settings-schema` 新增 `editor` 段。
- `@ec/account`：auth-types / security（密码强度 + PKCE）/ session（并发刷新去重）/ binding / offline /
  auth-client / oauth/{google,github,wechat}，30 项测试。
- 渲染层：`features/workspace`（5 组件 + 仪表盘 3 组件 + 端口 + 联动事件，28 项）、
  `features/docs`（4 组件 + 端口，18 项）、`features/auth`（5 组件 + 端口，12 项）、
  `features/settings`（6 面板 + 端口，新增 22 项），路由 `/docs`、`/account` 与左导航入口已接。
- `services/account`（**新增 workspace 成员**）：Fastify + better-sqlite3 + zod，八个接口按 PRD §8，
  `Dockerfile` / `docker-compose.yml` / `openapi.yaml` / 一键启动脚本，10 项集成测试（`app.inject`）。

### 本轮踩坑与决策（写进 docs/tasks/00 的 Wave 9 备注）

1. **`@ec/ui` 的 `Table` 声明了 `Column.render` 却从不消费** → 单元格渲染 `[object Object]`、
   React 抛 `Objects are not valid as a React child`。已修为 `renderCell ?? col.render?.(row, i) ?? row[key]`。
   教训：给"自定义渲染"接口写实现前，先 grep 组件内部是否真的读了这个字段。
2. **D-02「无云端同步入口」断言要剥注释 + 跳过 `__tests__`**：注释里写"不出现云同步状态"、
   测试文件里的词表字面量都会自命中（本轮两次假红）。
3. **`@ec/core` 首次出现 Node 侧模块**（docs 的 docx/pdf 解析器用 `node:zlib`）→ core 增加
   `exports.browser` 条件入口（`src/browser.ts`），排除这三个文件；core 引 `@ec/data` 只取 `newUlid`
   （data 的 browser 入口只导 ids 纯函数）。
4. **跨包依赖方向**：core 不能反向依赖 `@ec/pipeline`（pipeline 依赖 core）→ 七端常量在 core 侧镜像，
   渲染层做对齐断言；FR-AI-13 矩阵在渲染层直接消费 pipeline 的 `PLATFORM_MATRIX`（与 T5-04 问卷同源）。
5. **联动用轻量事件总线**而非互相 import：`features/workspace/workspace-events.ts` 的
   `emitTargetsChanged` 载荷带 `@ec/designer` 推导的画布预设（presetId + 宽高）与组件库分组。
6. **服务端技术栈落地口径**：本机无 PostgreSQL 且不宜拉 Prisma 引擎 → Fastify + SQLite，
   JWT/scrypt 全用 `node:crypto` 手写；已移除接口用 grep 断言。

### 子代理协作实况

- 子代理 A 独立完成 T9-06（services/account，10 项测试，自测通过）。
- 子代理 B（T9-04）在 **429 限流**中被打断：`packages/core/src/docs/**` 已落盘且 22 项测试全绿（质量可用），
  但 `apps/renderer/src/features/docs/**` 完全没做 → 主会话接手补全渲染层 4 组件 + 假端口 + 18 项测试。
  另外发现 core/docs 的 `index.ts` 漏导出 `parsers/browser-registry`（tsc 报 `createBrowserParserRegistry` 不存在），已补。
- 教训复现：限流在子代理创建后才失败，接手前必须先 `ls` + 跑该目录测试判断缺口（本轮省了大量重做）。

### 待 Wave 10

真实外壳装配（`__EC_WORKSPACE__` / `__EC_DOCS__` / `__EC_AUTH__` / `__EC_SETTINGS__` 四端口 + 设计器联动订阅）、
E2E-01/02/04/13/14 手工走查、docx/pdf 真实文件覆盖面（零依赖手写解析的边界）、图片 OCR 接入、
`services/account` 的容器化与 PostgreSQL 版验证。
