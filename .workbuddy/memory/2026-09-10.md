# 2026-09-10 工作日志

## Wave 3 可视化设计器（T3-01~T3-11）全部落地

按 `docs/tasks/00-通用上下文与执行约定.md` + `docs/tasks/04-Wave3-可视化设计器.md` 完成设计器 Wave。

**规模**：`packages/designer` 从 1 个占位入口扩展到 24 个测试文件 / 86 个源文件；
designer 355 测试 + 渲染层设计器集成 9 测试；全仓 **939 测试全绿**、lint 零问题、各包 tsc 零错误。

**冻结的跨任务契约（后续 Wave 必读）**
- `dsl/*`：PageDsl / ElementNode / traverse / schema(zod) / serialize(原子写) / version(迁移链)
  - `ElementNode` 扩展字段：`name`(中文显示名)、`featureRef`、`locked`、`hidden`、`masterRef`、
    `responsive`（断点差异属性）、`condition`、`permission`
  - 文件信封 `{ dslVersion, page }`；`DSL_VERSION = 3`，v1→v2→v3 迁移链已测
- `store/*`：编辑器内核（zustand + @ec/core UndoManager 的 patch 级撤销）
  - `apply(label, recipe(draft: PageDsl), {coalesceKey})` 是所有写的唯一入口
  - `ports.ts` 定义 DesignerPorts（files / memory / projectMemory / design / requester / apiCatalog / flow / clock / isIdle），
    **设计器不依赖 @ec/memory**（避免 better-sqlite3 污染浏览器构建）
- `shared/*`：`condition.ts`（结构化条件，无 eval）、`expression.ts`（路径与模板）、`data-source.ts`（绑定路径目录）
- `registry/*`：属性 JSON Schema（14 种控件类型）+ 组件注册表（15 类内置组件，T3-04）

**性能实测（jsdom 口径，非真实帧率，报告里已注明方法论）**
- 500 元素：首屏渲染 500 节点；选中/hover 增量仅 **2 个节点**（未整树重渲染）
- 500 邻域吸附单帧 **0.26ms**（较 16.6ms 预算 ≈3837 FPS 上限）；8 层嵌套单帧 0.005ms
- 500 节点图层树实际渲染 **34 行**（虚拟化）
- 100 次快照总计 **23.6KB**（5 基线 + 95 增量），相比每次全量节省约 357KB
- 四断点全覆盖：实际 DSL 4054B vs 全量副本 16792B，**节省 75.9%**

**修复的既有缺陷**
1. `@ec/core` `undo-manager`：显式 `coalesceKey` 被拿去和 `label` 比较，导致合并键永不生效
   → `UndoEntry` 增加 `coalesceKey` 字段（属性面板 200ms 防抖依赖它）
2. `canvas/Canvas.tsx`：memo 在根节点命中会阻断子树 props 更新（选中态下传丢失）
   → 改为 `useMemo` 内自顶向下构建元素树 + 叶子 childrenContent 常量
3. T3-01 登录页夹具用了未注册组件类型（NavBar/Logo/Checkbox…）→ 统一为内置 15 类，仍恰好 20 元素
4. 子代理产出的若干 strict 类型/异常用法问题（`import()` 类型注解、`any`、exactOptionalPropertyTypes）

**未解决（交接给 Wave 9/10）**
- 渲染层 `vite build` 仍然失败：Wave 2 的记忆中心在运行时 import `@ec/memory`，
  经 `@ec/data` 拉入 `better-sqlite3` / `node:fs`。已用探针构建证明 **@ec/designer 自身无 Node 泄漏**
  （222 modules 打包成功）。修法建议：把 pure 常量/纯函数拆独立入口，或给 memory/data 标 `sideEffects`
- 设计器的真实端口装配（DSL 落盘、页面记忆写入、AI 生成、预览 requester）待外壳注入
- Tauri/Electron 侧未联调

---

## Wave 4：上下文引擎与代码生成（T4-01~T4-06，2026-09-10 晚落地）

**交付**：全仓 1162 测试全绿（120 文件）、lint --max-warnings 0、ai/designer/renderer 三包 tsc 零错。
- designer 411（+56 备注系统）、ai 242（+143：context/generate/write/anchors）、renderer 81（+24：ContextPanel 9 + CodeView 15）

**新增子域与关键决策**
- `packages/designer/src/notes/*`：三级备注（元素/页面/功能）+ 六类型（禁止事项=硬约束，优先级锁 P5 且不可人工下调）
  - `NoteRepository.getNotesForContext(target)` / `hasNoteUpdatedSince(target, since)` 是 T4-02/T4-04 的消费契约
  - 富文本 = RichTextDocument（paragraph/heading/bullet/ordered + 行内 marks），`toggleMarkInSpans` 纯函数可测
  - LayerNode/LayerTree 加了**可选** `note`/`noteBadges` prop（不破坏 Wave 3 测试）；画布角标走 `ElementNoteBadges` 浮层（DOM 测量，不侵入 Canvas memo 结构）
- `packages/ai/src/context/*`：八类块（FR-AI-01）+ 指令/依赖契约两个工程块，配额表与裁剪优先级在 `token-budget.ts`
  - **不依赖 @ec/memory/@ec/designer**：五数据源全走 `ContextSources` 端口，缺端口→逐块优雅跳过（`AssembledContext.skipped`）
  - 硬约束（禁止事项）在**裁剪前**抽取，预算砍掉备注块也不会丢（`extractHardConstraints`）
  - 配额和(143k) > 总预算(128k) 是设计使然：配额是单块上限，预算才硬约束
  - 基准：1000 条记忆组装冷 5.9ms / 热 p95 5.9ms（预算 300ms，node 口径，不含网络/tokenizer 精确计数）
- `packages/ai/src/generate/*`：九类模板 + 输出契约（zod）+ 四级解析（json→fenced-json→code-blocks→raw）
  - 解析成功率 19/20=95%（20 组异常样本，含围栏/夹带文本/截断/契约违规/纯散文）
  - 坑：output-schema 全字段 `.default()` 导致空对象 `{}` 也能过 zod → 必须 refine"至少一项实质内容"，
    否则代码块里的 `class A {}` 会被误判成合法输出、把真降级路径挡掉
  - 降级提取代码块时要**跳过 json 围栏**（那是契约载荷不是产物文件）
- `packages/ai/src/write/*`：plan（不落盘+冲突检测）→ preview（按文件/块选择）→ apply（快照事务回滚）
  - patch=unified diff，按上下文定位容忍行号漂移；定位失败宁可拒绝不做部分应用
  - `read-only-guard`：运行时拦截（键入/粘贴/拖拽/剪切）+ `scanReadOnlyCompliance` 静态扫描（**先剥注释再匹配**，
    否则文档注释里写"不使用 contentEditable"会误报）；测试直接扫 CodeView.tsx 源码断言零违规
  - `external-change-watcher`：真实 fs.watch + **独立子进程写文件**验证（execFileSync）；
    Windows 下 `FSWatcher.close(cb)` 回调不触发 → stop() 带 2s 兜底（勿依赖关闭回调）
- `packages/ai/src/anchors/*`：三重锚定（声明+注释标记 `@everyonecoding:anchor`+AST 校验）
  - **AstAdapter 是端口，默认"多语言符号索引器"（正则+括号/缩进配平）而非 ts-morph**——ts-morph 会把
    TS 编译器拖进 ai/browser.ts 的浏览器构建口；外壳（Wave 9/10）可注入 ts-morph 实现，接口已留好
  - 坑：候选分先封顶(min 1)再排序，会让「容器内改名」与「容器类本身」并列 1.0、字典序兜底把类排前面
    → **排序必须用未封顶 raw**，封顶只作用于展示值
  - 锚点行与 PRD §6.2 `code_anchor` 逐列一致（测试直接比对 `TABLE_COLUMNS.code_anchor`）
- 渲染层新端口：`globalThis.__EC_AI_CONTEXT__`（ContextPanelApi）、`globalThis.__EC_CODE__`（CodeViewApi），
  与 `__EC_MEMORY__` 同一套做法；组件测试用**真实引擎+假端口**，不手搓 AssembledContext
- ContextPanel 的自动组装 effect 依赖 requestKey=JSON.stringify(request)（调用方常在渲染期新建请求对象，
  依赖对象本身会死循环组装）

**未解决（交接 Wave 9/10）**
- ContextSources 五端口（记忆检索/备注/设计器 DSL/文档/代码索引）的外壳装配
- WritePipeline 的 WorkspaceFileSystem（适配 @ec/core FileService）、ExternalChangeWatcher 的 fs.watch 装配
- ts-morph AstAdapter 注入、E2E-05（选中元素生成后端代码）与 E2E-18（只读拦截）手工走查
- 渲染层 vite build 失败（Wave 2 遗留，与 Wave 4 无关）
