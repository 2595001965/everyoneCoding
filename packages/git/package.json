{
  "name": "@ec/git",
  "version": "0.1.0",
  "private": true,
  "description": "Git 封装（git2 优先，回退系统 CLI）",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "browser": "./src/browser.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@ec/core": "workspace:*",
    "@ec/shell-api": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.4"
  }
}
