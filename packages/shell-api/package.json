{
  "name": "@ec/shell-api",
  "version": "0.1.0",
  "private": true,
  "description": "外壳抽象接口 + 工厂 + Mock 实现（Tauri / Electron 双形态共用）",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "vitest": "^2.1.4"
  }
}
